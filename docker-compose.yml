version: "3.9"

services:
  redis:
    image: redis:alpine
    container_name: cmx_redis_qaportal_ph
    ports:
      - "5009:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]
    restart: always

volumes:
  redis_data:
